// Swap this once the real backend exists (see apps/backend)
export const API_URL = 'https://api.example.com';

export const POINTS_PER_WIN = 10;

export const CHALLENGE_STATUS = {
  ACTIVE: 'active',
  PENDING: 'pending',
  COMPLETED: 'completed',
};
